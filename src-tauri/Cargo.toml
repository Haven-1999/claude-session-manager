[package]
name = "claude-session-manager"
version = "0.1.0"
description = "Claude Session Manager Mac App"
authors = ["csm"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url = "2"
libc = "0.2"
